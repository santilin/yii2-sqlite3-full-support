# renameColumn: Fallan los índices.
# virtual fields en todos los select * into como en addForeignKey
  - dropForeignKey
  - alterColumn
  - addPrimaryKey
  - dropColumn
# truncateTable: resetSequence
